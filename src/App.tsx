import './App.css';
import { LoggedIn } from './components/LoggedIn';

function App() {
    // const auth = useSelector((state: RootState) => state.auth.token);
    return <LoggedIn />;
}

export default App;
