import React from 'react';
import LoginButton from './LoginButton';

const Login: React.FC = () => {
    return (
        <div>
            <h1>Login</h1>

            <LoginButton />
        </div>
    );
};

export default Login;
